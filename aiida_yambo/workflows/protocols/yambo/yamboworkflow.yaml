default_inputs:
    meta_parameters:
        qe_protocol: 'fast'
    yres:
        protocol:'GW_fast'
default_protocol: GW_fast
protocols:
    GW_fast:
        description: 'GW fast Protocol, under converged for most materials, but fast'
    GW_moderate:
        description: 'GW moderate Protocol, meta converged for most materials, higher computation cost than GW_fast'
        meta_parameters:
            qe_protocol: 'moderate'